<UserControl x:Class="MoniHop.Desktop.Views.DisplaysPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer Focusable="False"
                  FocusVisualStyle="{x:Null}"
                  HorizontalScrollBarVisibility="Disabled"
                  VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <TextBlock Style="{StaticResource PageTitleStyle}" Text="显示器" />
            <TextBlock Margin="0,8,0,22" Style="{StaticResource SecondaryTextStyle}"
                       Text="查看当前系统识别到的显示器及基础信息。" />

            <Border Style="{StaticResource SurfaceSectionStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource SectionTitleStyle}" Text="当前显示器" />
                    <ItemsControl ItemsSource="{Binding ConnectedDisplays}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate><WrapPanel HorizontalAlignment="Center" /></ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="206" Height="112" Margin="6" Padding="16"
                                        Background="{StaticResource BrandSubtleBrush}"
                                        BorderBrush="{StaticResource BrandBrush}" BorderThickness="1" CornerRadius="5">
                                    <StackPanel>
                                        <TextBlock FontSize="18" FontWeight="SemiBold" Text="{Binding Name}" TextTrimming="CharacterEllipsis" />
                                        <TextBlock Margin="0,8,0,0" Text="{Binding ScreenLabel}" />
                                        <TextBlock Margin="0,4,0,0" Style="{StaticResource SecondaryTextStyle}" Text="{Binding PrimaryStatus}" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <TextBlock Margin="0,24,0,10" Style="{StaticResource SectionTitleStyle}" Text="显示器列表" />
            <TextBlock Margin="0,0,0,12" Style="{StaticResource SecondaryTextStyle}"
                       Text="列出当前及以前连接过的显示器；插拔后会自动刷新连接状态。" />
            <TextBlock x:Name="RefreshStatusText" Margin="0,0,0,10" Style="{StaticResource SecondaryTextStyle}" />
            <ListView ItemsSource="{Binding AllDisplays}"
                      MinHeight="150"
                      AlternationCount="2"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.VerticalScrollBarVisibility="Disabled">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="MinHeight" Value="46" />
                        <Setter Property="Padding" Value="6,0" />
                        <Setter Property="VerticalContentAlignment" Value="Center" />
                        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
                        <Setter Property="BorderThickness" Value="0,0,0,1" />
                        <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
                        <Style.Triggers>
                            <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                <Setter Property="Background" Value="#FAFBFC" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource BrandSubtleBrush}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="显示器名称" Width="140"><GridViewColumn.CellTemplate><DataTemplate><TextBlock VerticalAlignment="Center" FontWeight="SemiBold" Text="{Binding Name}" TextTrimming="CharacterEllipsis" ToolTip="{Binding Name}" /></DataTemplate></GridViewColumn.CellTemplate></GridViewColumn>
                        <GridViewColumn Header="属性" Width="110" DisplayMemberBinding="{Binding PrimaryStatus}" />
                        <GridViewColumn Header="分辨率" Width="105" DisplayMemberBinding="{Binding Resolution}" />
                        <GridViewColumn Header="刷新率" Width="70" DisplayMemberBinding="{Binding RefreshRate}" />
                        <GridViewColumn Header="缩放" Width="55" DisplayMemberBinding="{Binding Scale}" />
                        <GridViewColumn Header="方向" Width="75" DisplayMemberBinding="{Binding Orientation}" />
                        <GridViewColumn Header="尺寸" Width="90"><GridViewColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding PhysicalSize}" ToolTip="{Binding PhysicalSizeDetail}" /></DataTemplate></GridViewColumn.CellTemplate></GridViewColumn>
                        <GridViewColumn Header="应用规则" Width="75" DisplayMemberBinding="{Binding ApplicationRules}" />
                        <GridViewColumn Header="状态" Width="70"><GridViewColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding ConnectionStatus}"><TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Foreground" Value="{StaticResource SuccessBrush}" /><Style.Triggers><DataTrigger Binding="{Binding IsConnected}" Value="False"><Setter Property="Foreground" Value="{StaticResource WarningBrush}" /></DataTrigger></Style.Triggers></Style></TextBlock.Style></TextBlock></DataTemplate></GridViewColumn.CellTemplate></GridViewColumn>
                        <GridViewColumn Header="操作" Width="180"><GridViewColumn.CellTemplate><DataTemplate><StackPanel Orientation="Horizontal"><Button Content="重命名" Tag="{Binding}" Click="Rename_OnClick" Style="{StaticResource SecondaryButtonStyle}" /><Button Margin="6,0,0,0" Content="忘记" Tag="{Binding}" Click="Forget_OnClick" IsEnabled="{Binding IsConnected, Converter={StaticResource InverseBooleanConverter}}" Style="{StaticResource SecondaryButtonStyle}" ToolTip="仅删除 MoniHop 本地记录" /></StackPanel></DataTemplate></GridViewColumn.CellTemplate></GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
    </ScrollViewer>
</UserControl>
